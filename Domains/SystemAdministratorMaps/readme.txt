Note that for proper performance (e.g. ring structures have a single computer whose reward is double),
map names must be of the form 'xxx.txt', with any identifiers such as 'ring' at the end.
e.g. 8Ring.txt will perform fine, but 8Ring.map or Ring8.txt will not.