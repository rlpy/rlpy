Tasks:
- Everything in dot format rather than * or outer. All items has to be ndarray rather than array.
- Doxygen
- Fix the issue with legend not being shown during the run
- Config File For Experiments
- Tools.py Arrangement
- Optimization
- Feature Names
- bestAction in matrix form (how about breaking ties randomly)
- Update Visual for PST
- Improve installation process; automate setting of platform-dependent environment variables, either during installation or at runtime from Tools.py
- Remove SHOW_FINAL_PLOT from the main and all places that it will affect (scripts, multipleRuns, etc.)
- PST description is wrong (Transpose)
- Make arrows in the pitmaze to be bounded in each grid, regardless of window size.